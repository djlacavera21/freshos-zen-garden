# Official Flavor — FreshOS Zen Garden

This is the default profile of FreshOS Zen Garden Edition. Applying it on a system that already *is* the base is a no-op except for ensuring services and theme tokens exist.

Use this flavor as the parent when you author a community overlay: copy the YAML, change `metadata.id`, narrow `modules`, and add overlay files.
